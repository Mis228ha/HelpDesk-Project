# Django and its pinned minor version (4.2 LTS)
Django==4.2.*

# ASGI interface (installed automatically with Django,
# pinned explicitly for reproducible builds)
asgiref==3.12.1

# SQL parser used by Django ORM (Django dependency)
sqlparse==0.6.0

# Forms: render forms without hand-coding every field's HTML
django-crispy-forms==2.5

# Settings via .env (SECRET_KEY, DEBUG, DATABASE_URL, etc.)
django-environ==0.14.0

# --- Needed later, when switching to PostgreSQL ---
# psycopg2-binary==2.9.9
