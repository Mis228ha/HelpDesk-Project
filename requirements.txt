# Django и его точная минорная версия (4.2 LTS)
Django==4.2.*

# ASGI-интерфейс (устанавливается автоматически вместе с Django,
# зафиксирован явно для воспроизводимости сборки)
asgiref==3.12.1

# SQL-парсер, используется Django ORM (зависимость Django)
sqlparse==0.6.0

# Формы: рендеринг форм без ручной вёрстки каждого поля
django-crispy-forms==2.5

# Настройки через .env (SECRET_KEY, DEBUG, DATABASE_URL и т.д.)
django-environ==0.14.0

# --- Понадобится позже, при переходе на PostgreSQL ---
# psycopg2-binary==2.9.9
